import styled from '@emotion/styled';

export const LoaderContainer = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
`;

export const LoaderHeader = styled.p`
  font-size: 30px;
  font-weight: 700;
  line-height: 1.5;
  margin-bottom: 20px;
  color: var(--second-accent-color);
`;
